 wap to find minimum value from the two lists in python.
    ex:
        input:
        2 5 7 8 9
        3 4 7 8 1
        output:
            1


        code:-

    class solution:
        def singleNumber(self,nums:list[int])->int:
            n=len(nums)
            for i in range(0,n):
                count=0
            for j in range(0,n):
                if nums[i]==nums[j]:
                    count=counnt+1
                if count==1:
                    return nums[i]
